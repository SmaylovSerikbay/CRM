# ⚠️ ВАЖНО ДЛЯ ПОЛЬЗОВАТЕЛЕЙ WINDOWS

## НЕ используйте `make` на Windows!

Make на Windows имеет проблемы:
- ❌ Кириллица отображается как "╨Ъ╨╛╨╝╨╝╨╕╤В"
- ❌ Команда `read` не работает
- ❌ Пути SSH ключей работают некорректно
- ❌ Escape-последовательности цветов не работают

## ✅ Используйте .bat скрипты!

### Настройка (один раз)

**CMD (рекомендуется):**
```cmd
setup-ssh.bat
```

**PowerShell:**
```powershell
.\setup-ssh.bat
```

### Деплой

**CMD:**
```cmd
deploy.bat "описание изменений"
deploy-quick.bat "описание изменений"
server-logs.bat
server-connect.bat
```

**PowerShell:**
```powershell
.\deploy.bat "описание изменений"
.\deploy-quick.bat "описание изменений"
.\server-logs.bat
.\server-connect.bat
```

## Почему CMD лучше PowerShell?

В CMD не нужно добавлять `.\` перед скриптами:

```cmd
# CMD - просто и понятно
setup-ssh.bat
deploy.bat "fix"

# PowerShell - нужно добавлять .\
.\setup-ssh.bat
.\deploy.bat "fix"
```

## Как открыть CMD?

1. Нажмите `Win + R`
2. Введите `cmd`
3. Нажмите Enter
4. Перейдите в папку проекта:
   ```cmd
   cd D:\projectspython\CRM
   ```

## Если всё равно хотите использовать make

Make будет работать, но:
- Сообщения будут на английском (без кириллицы)
- Не будет интерактивного ввода сообщения коммита
- Могут быть проблемы с путями

Команды:
```bash
make deploy        # Работает, но без красивого вывода
make deploy-quick  # Работает
make server-logs   # Работает
```

## Рекомендация

**Используйте .bat скрипты в CMD** - это самый простой и надежный способ для Windows!

---

📖 **Полная документация:** [START_HERE.md](./START_HERE.md)
