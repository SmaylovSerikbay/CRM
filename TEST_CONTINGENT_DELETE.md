# Тест удаления контингента

## Цель
Проверить правильность работы функции удаления контингента (отдельных сотрудников и всех сразу).

## Предварительные условия
1. ✅ Бэкенд запущен (http://localhost:8001)
2. ✅ Фронтенд запущен (http://localhost:3001)
3. ✅ Есть пользователь с ролью "clinic" или "employer"
4. ✅ В базе есть контингент для тестирования

## Реализация

### Бэкенд (Django)
- **Модель**: `ContingentEmployee` в `backend/api/models.py`
- **ViewSet**: `ContingentEmployeeViewSet` в `backend/api/views.py`
- **Эндпоинты**:
  - `DELETE /api/contingent-employees/{id}/` - удаление одного сотрудника (стандартный метод `destroy`)
  - `DELETE /api/contingent-employees/delete_all/?user_id={user_id}` - удаление всех сотрудников пользователя

### Фронтенд (Next.js + React)
- **API Client**: `frontend/lib/api/client.ts`
  - `deleteContingentEmployee(userId, employeeId)` - DELETE запрос на `/contingent-employees/{id}/`
  - `deleteAllContingentEmployees(userId)` - DELETE запрос на `/contingent-employees/delete_all/?user_id={userId}`

- **Store API**: `frontend/lib/store/workflow-store-api.ts`
  - `deleteContingentEmployee(employeeId)` - обертка над API client
  - `deleteAllContingentEmployees()` - обертка над API client

- **UI компоненты**:
  - `frontend/app/dashboard/clinic/contingent/page.tsx` - страница контингента для клиники
  - `frontend/app/dashboard/employer/contingent/page.tsx` - страница контингента для работодателя
  - `frontend/app/dashboard/clinic/contracts/page.tsx` - управление контингентом в договорах

## Тестовые сценарии

### Сценарий 1: Удаление одного сотрудника
1. Открыть страницу контингента
2. Найти сотрудника в списке
3. Нажать кнопку "Удалить" (иконка корзины)
4. Подтвердить удаление в диалоге
5. **Ожидаемый результат**: 
   - Сотрудник удален из базы данных
   - Список обновлен без удаленного сотрудника
   - Показано уведомление "Сотрудник успешно удален"

### Сценарий 2: Удаление всех сотрудников
1. Открыть страницу контингента
2. Нажать кнопку "Удалить всех"
3. Подтвердить удаление в диалоге
4. **Ожидаемый результат**:
   - Все сотрудники пользователя удалены из базы данных
   - Список пуст
   - Показано уведомление "Все сотрудники удалены"

### Сценарий 3: Отмена удаления
1. Открыть страницу контингента
2. Нажать кнопку "Удалить" для сотрудника
3. Нажать "Отмена" в диалоге подтверждения
4. **Ожидаемый результат**:
   - Сотрудник НЕ удален
   - Список не изменился

### Сценарий 4: Удаление с ошибкой
1. Попытаться удалить несуществующего сотрудника
2. **Ожидаемый результат**:
   - Показано сообщение об ошибке
   - Список не изменился

## Проверка через API

### Проверка удаления одного сотрудника
```bash
# 1. Получить список контингента
curl -X GET "http://localhost:8001/api/contingent-employees/?user_id=1"

# 2. Удалить сотрудника (замените {id} на реальный ID)
curl -X DELETE "http://localhost:8001/api/contingent-employees/{id}/"

# 3. Проверить, что сотрудник удален
curl -X GET "http://localhost:8001/api/contingent-employees/?user_id=1"
```

### Проверка удаления всех сотрудников
```bash
# 1. Получить количество сотрудников
curl -X GET "http://localhost:8001/api/contingent-employees/?user_id=1"

# 2. Удалить всех сотрудников
curl -X DELETE "http://localhost:8001/api/contingent-employees/delete_all/?user_id=1"

# 3. Проверить, что список пуст
curl -X GET "http://localhost:8001/api/contingent-employees/?user_id=1"
```

## Проверка через UI

### Шаги для ручного тестирования:
1. Войти в систему как клиника или работодатель
2. Перейти в раздел "Контингент"
3. Убедиться, что есть сотрудники в списке
4. Протестировать удаление одного сотрудника:
   - Нажать иконку корзины рядом с сотрудником
   - Подтвердить удаление
   - Проверить, что сотрудник исчез из списка
5. Протестировать удаление всех:
   - Нажать кнопку "Удалить всех"
   - Подтвердить удаление
   - Проверить, что список пуст

## Проверка в базе данных

```bash
# Подключиться к контейнеру бэкенда
docker compose exec backend python manage.py shell

# В shell Django:
from api.models import ContingentEmployee, User

# Получить пользователя
user = User.objects.get(id=1)

# Посмотреть количество сотрудников
print(f"Всего сотрудников: {ContingentEmployee.objects.filter(user=user).count()}")

# Удалить одного сотрудника
employee = ContingentEmployee.objects.filter(user=user).first()
if employee:
    employee_id = employee.id
    employee.delete()
    print(f"Удален сотрудник ID: {employee_id}")

# Проверить количество после удаления
print(f"Осталось сотрудников: {ContingentEmployee.objects.filter(user=user).count()}")

# Удалить всех сотрудников
count = ContingentEmployee.objects.filter(user=user).delete()
print(f"Удалено сотрудников: {count[0]}")
```

## Возможные проблемы

### 1. Ошибка 404 при удалении
**Причина**: Сотрудник не найден или уже удален
**Решение**: Проверить, что ID сотрудника существует в базе

### 2. Ошибка 403 Forbidden
**Причина**: Пользователь не авторизован или нет прав
**Решение**: Проверить авторизацию и права доступа

### 3. Список не обновляется после удаления
**Причина**: Не вызывается обновление списка после удаления
**Решение**: Проверить, что после `deleteContingentEmployee` вызывается `getContingent()`

### 4. Удаляются сотрудники других пользователей
**Причина**: Неправильная фильтрация в `delete_all`
**Решение**: Проверить, что в `delete_all` используется фильтр `user=user`

## Результаты тестирования

### ✅ Что работает:
- [ ] Удаление одного сотрудника через UI
- [ ] Удаление всех сотрудников через UI
- [ ] Подтверждение удаления
- [ ] Обновление списка после удаления
- [ ] Уведомления об успешном удалении
- [ ] Обработка ошибок

### ❌ Что не работает:
- [ ] (заполнить после тестирования)

### 🔧 Что нужно исправить:
- [ ] (заполнить после тестирования)

## Следующие шаги
1. Запустить приложение
2. Выполнить все тестовые сценарии
3. Отметить результаты
4. Исправить найденные проблемы
5. Повторить тестирование
