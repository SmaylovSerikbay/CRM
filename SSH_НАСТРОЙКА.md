# Быстрая настройка SSH подключения без пароля

## 🔑 Способ 1: Автоматический (рекомендуется)

Просто запустите файл:
```
setup-ssh-key-simple.bat
```

Скрипт покажет ваш ключ и попытается автоматически скопировать его на сервер.
Вам нужно будет один раз ввести пароль сервера.

## 📋 Способ 2: Ручной (если автоматический не работает)

### Шаг 1: Откройте ваш публичный ключ

```cmd
notepad %USERPROFILE%\.ssh\id_rsa.pub
```

**Скопируйте всё содержимое** (это одна длинная строка вида `ssh-rsa AAAAB3...`)

### Шаг 2: Подключитесь к серверу

```cmd
ssh ubuntu@82.115.48.40
```

**Пароль:** `q+I/U9UzOPuXexTC8jbyHgs=`

### Шаг 3: На сервере выполните команды

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
```

### Шаг 4: Вставьте ключ в nano

1. Нажмите `Ctrl+Shift+V` (вставить в Windows Terminal) или просто вставьте через правую кнопку мыши
2. Нажмите `Ctrl+O` (сохранить)
3. Нажмите `Enter` (подтвердить имя файла)
4. Нажмите `Ctrl+X` (выйти из nano)

### Шаг 5: Установите права

```bash
chmod 600 ~/.ssh/authorized_keys
exit
```

### Шаг 6: Проверьте!

```cmd
ssh ubuntu@82.115.48.40
```

Теперь должно подключиться **без пароля**! ✅

---

## 📝 Параметры сервера

- **IP:** 82.115.48.40
- **Пользователь:** ubuntu
- **Пароль:** q+I/U9UzOPuXexTC8jbyHgs=

---

## ❓ Решение проблем

### Если всё равно просит пароль:

Проверьте на сервере:
```bash
ls -la ~/.ssh/
```

Должно быть:
```
drwx------ 2 ubuntu ubuntu 4096 ... .ssh
-rw------- 1 ubuntu ubuntu  400 ... authorized_keys
```

Если права неправильные, исправьте:
```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Если ключ не добавляется:

Проверьте содержимое файла на сервере:
```bash
cat ~/.ssh/authorized_keys
```

Там должен быть ваш ключ (та же строка из `id_rsa.pub`)

---

## ✅ После настройки

Теперь можно использовать:

```cmd
ssh ubuntu@82.115.48.40
```

Без ввода пароля! 🎉

Для деплоя используйте `.bat` скрипты:
- `deploy.bat`
- `deploy-quick.bat`
- `server-logs.bat`
- `server-connect.bat`

