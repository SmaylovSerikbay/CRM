# Git - Быстрая шпаргалка для команды

## Ежедневная работа

### Начало дня
```bash
git pull origin main
```

### Во время работы (каждые 30-60 мин)
```bash
git status                    # Проверить изменения
git add <файлы>              # Добавить файлы
git commit -m "Описание"     # Закоммитить
git pull origin main         # Обновить
git push origin main         # Запушить
```

### Конец дня
```bash
git add .
git commit -m "End of day: что сделано"
git pull origin main
git push origin main
```

## Работа с ветками (для больших задач)

```bash
# Создать ветку
git checkout -b feature/my-task

# Работать в ветке
git add .
git commit -m "Описание"

# Влить в main
git checkout main
git pull origin main
git merge feature/my-task
git push origin main

# Удалить ветку
git branch -d feature/my-task
```

## Разрешение конфликтов

```bash
# При конфликте:
# 1. Открыть файл
# 2. Найти <<<<<<< HEAD и >>>>>>> origin/main
# 3. Выбрать нужный код
# 4. Удалить маркеры
git add <файл>
git commit -m "Merge: resolve conflict"
git push origin main
```

## Полезные команды

```bash
git status                    # Статус
git log --oneline -10        # История
git diff                     # Изменения
git stash                    # Сохранить изменения
git stash pop                # Вернуть изменения
git reset --hard origin/main # Сбросить все (ОСТОРОЖНО!)
```

## Главные правила

1. ✅ Всегда `git pull` перед началом работы
2. ✅ Часто коммитьте и пушьте (каждые 30-60 мин)
3. ✅ Пишите понятные сообщения коммитов
4. ✅ Общайтесь перед изменением общих файлов
5. ✅ Используйте ветки для больших задач

## Разделение работы

**Разработчик 1:**
- `frontend/app/dashboard/clinic/*`
- `backend/api/views.py` (clinic)

**Разработчик 2:**
- `frontend/app/dashboard/employer/*`
- `backend/api/serializers.py`

**Общие файлы (координация!):**
- `frontend/lib/store/*`
- `frontend/lib/api/*`
- `backend/api/models.py`

## Экстренные ситуации

### Отменить изменения в файле
```bash
git checkout -- <файл>
```

### Отменить последний коммит
```bash
git reset --soft HEAD~1  # Сохранить изменения
git reset --hard HEAD~1  # Удалить изменения
```

### Отменить слияние
```bash
git merge --abort
```

### Сохранить работу и переключиться
```bash
git stash
# ... делаете другую работу ...
git stash pop
```

---

📖 Подробное руководство: `docs/GIT_WORKFLOW_TEAM.md`
