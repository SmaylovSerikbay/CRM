# 🐛 Руководство по отладке через URL

## 🎯 Проблема решена!

Теперь каждая функция имеет свой уникальный URL, что позволяет:
- **Быстро находить баги** по URL
- **Делиться ссылками** на конкретные разделы
- **Сохранять закладки** для быстрого доступа
- **Изолировать проблемы** в конкретных компонентах

## 📍 Новая структура URL

### Основные разделы
```
/dashboard/clinic                           → Главная панель
/dashboard/clinic/contracts                 → Список всех договоров
/dashboard/clinic/contingent               → Весь контингент
/dashboard/clinic/calendar-plan            → Все календарные планы
/dashboard/clinic/route-sheets             → Все маршрутные листы
/dashboard/clinic/doctors                  → Список врачей
/dashboard/clinic/harmful-factors          → Справочник вредных факторов
```

### Работа с конкретным договором
```
/dashboard/clinic/contracts/123                    → Детали договора #123
/dashboard/clinic/contracts/123/contingent         → Контингент договора #123
/dashboard/clinic/contracts/123/calendar-plan      → Планы договора #123
/dashboard/clinic/contracts/123/route-sheets       → Маршруты договора #123
```

## 🔍 Как использовать для отладки

### 1. При возникновении бага
```
❌ Раньше: "Ошибка в модальном окне контингента"
✅ Теперь: "Ошибка на /dashboard/clinic/contracts/123/contingent"
```

### 2. Поиск кода по URL
```
URL: /dashboard/clinic/contracts/123/contingent
Файл: frontend/app/dashboard/clinic/contracts/[contractId]/contingent/page.tsx
```

### 3. Быстрое воспроизведение бага
```
1. Скопируйте URL где произошла ошибка
2. Откройте этот URL в браузере
3. Баг воспроизведется на той же странице
```

## 📂 Соответствие URL → Файлы

| URL | Файл | Описание |
|-----|------|----------|
| `/dashboard/clinic/contracts` | `contracts/page.tsx` | Список договоров |
| `/dashboard/clinic/contracts/[id]` | `contracts/[contractId]/page.tsx` | Детали договора |
| `/dashboard/clinic/contracts/[id]/contingent` | `contracts/[contractId]/contingent/page.tsx` | Контингент договора |
| `/dashboard/clinic/contingent` | `contingent/page.tsx` | Весь контингент |
| `/dashboard/clinic/calendar-plan` | `calendar-plan/page.tsx` | Все планы |
| `/dashboard/clinic/route-sheets` | `route-sheets/page.tsx` | Все маршруты |
| `/dashboard/clinic/doctors` | `doctors/page.tsx` | Врачи |
| `/dashboard/clinic/harmful-factors` | `harmful-factors/page.tsx` | Вредные факторы |

## ⚡ Преимущества новой архитектуры

### Быстрая отладка
- **Точный адрес проблемы**: URL сразу указывает на проблемный компонент
- **Изолированная загрузка**: каждая страница загружает только свои данные
- **Независимые компоненты**: баг в одном разделе не влияет на другие

### Удобство разработки
- **Четкая структура**: каждая функция в своем файле
- **Легкое тестирование**: можно тестировать каждую страницу отдельно
- **Простое расширение**: добавление новых функций не затрагивает существующие

### Производительность
- **Ленивая загрузка**: данные загружаются только при необходимости
- **Кэширование**: данные сохраняются между переходами
- **Быстрая навигация**: нет задержек от загрузки ненужных данных

## 🚀 Примеры использования

### Сценарий 1: Баг в загрузке контингента
```
1. Пользователь: "Не загружается контингент в договоре №12345"
2. Разработчик: Открывает /dashboard/clinic/contracts/12345/contingent
3. Видит ошибку в консоли, знает что проблема в contracts/[contractId]/contingent/page.tsx
4. Исправляет баг в конкретном файле
```

### Сценарий 2: Медленная загрузка
```
1. Пользователь: "Медленно открывается список врачей"
2. Разработчик: Открывает /dashboard/clinic/doctors
3. Смотрит Network tab, видит медленный API запрос
4. Оптимизирует запрос в doctors/page.tsx
```

### Сценарий 3: Ошибка в календарном плане
```
1. Пользователь: "Ошибка при создании плана для договора №67890"
2. Разработчик: Переходит на /dashboard/clinic/contracts/67890/calendar-plan
3. Воспроизводит ошибку, видит проблему в конкретном компоненте
4. Исправляет в contracts/[contractId]/calendar-plan/page.tsx
```

## 🛠️ Инструменты отладки

### 1. URL как индикатор
- Всегда смотрите на URL при возникновении ошибки
- URL точно указывает на проблемный компонент

### 2. Прямые переходы
- Можете сразу перейти на проблемную страницу
- Не нужно кликать через несколько модальных окон

### 3. Изолированное тестирование
- Каждую страницу можно тестировать независимо
- Легко создавать тестовые сценарии

## 📞 Алгоритм решения проблем

```
1. Получили сообщение об ошибке
   ↓
2. Узнали URL где произошла ошибка
   ↓
3. Нашли соответствующий файл по таблице выше
   ↓
4. Открыли этот URL в браузере
   ↓
5. Воспроизвели ошибку
   ↓
6. Исправили в конкретном файле
   ↓
7. Протестировали на том же URL
```

---

**Результат**: Теперь отладка займет минуты вместо часов! 🎉