# Сравнение команд деплоя

## `make deploy-all` vs `make bg-auto`

### `make deploy-all` (РЕКОМЕНДУЕТСЯ для продакшена)
```bash
make deploy-all
```

**Что делает:**
1. ✅ **Git pull** - обновляет код из репозитория
2. ✅ **Быстрая сборка** (FAST_BUILD=1) - использует кэш Docker
3. ✅ **Автоматический деплой** - запускает новое окружение
4. ✅ **Переключение трафика** - автоматически или с подтверждением
5. ✅ **Очистка** - останавливает старое окружение

**Время:** ~2-3 минуты

**Когда использовать:**
- ✅ Обычные обновления кода
- ✅ Изменения в frontend/backend без изменения зависимостей
- ✅ Ежедневный деплой

---

### `make bg-auto` (Интерактивный режим)
```bash
make bg-auto
```

**Что делает:**
1. ❌ **БЕЗ git pull** - использует текущий код
2. ❓ **Спрашивает тип сборки:**
   - `1` - Быстрая (с кэшем)
   - `2` - Полная (без кэша)
3. ✅ **Автоматический деплой**
4. ✅ **Переключение трафика**
5. ✅ **Очистка**

**Время:** 
- Быстрая: ~2-3 минуты
- Полная: ~10-15 минут

**Когда использовать:**
- ✅ Когда код уже обновлен вручную
- ✅ Когда нужно выбрать тип сборки
- ✅ Для тестирования локальных изменений

---

## Все варианты команд

### 1. `make deploy-all` ⭐ РЕКОМЕНДУЕТСЯ
```bash
make deploy-all
```
- Git pull + быстрая сборка
- Автоматический режим
- **Время:** ~2-3 мин

### 2. `make bg-auto`
```bash
make bg-auto
```
- БЕЗ git pull
- Спрашивает тип сборки
- **Время:** ~2-15 мин (зависит от выбора)

### 3. `make bg-auto-fast`
```bash
make bg-auto-fast
```
- БЕЗ git pull
- Быстрая сборка (автоматически)
- **Время:** ~2-3 мин

### 4. `make bg-auto-full`
```bash
make bg-auto-full
```
- БЕЗ git pull
- Полная пересборка (автоматически)
- **Время:** ~10-15 мин

---

## Таблица сравнения

| Команда | Git Pull | Тип сборки | Время | Когда использовать |
|---------|----------|------------|-------|-------------------|
| `make deploy-all` ⭐ | ✅ Да | Быстрая (авто) | 2-3 мин | **Обычный деплой** |
| `make bg-auto` | ❌ Нет | Спрашивает | 2-15 мин | Локальные изменения |
| `make bg-auto-fast` | ❌ Нет | Быстрая (авто) | 2-3 мин | Без git pull |
| `make bg-auto-full` | ❌ Нет | Полная (авто) | 10-15 мин | Изменение зависимостей |

---

## Рекомендации

### Для продакшена (обычный деплой)
```bash
make deploy-all
```
Это самый удобный вариант - делает все автоматически.

### Если изменились зависимости (package.json, requirements.txt)
```bash
git pull
make bg-auto-full
```
Или просто:
```bash
make deploy-all  # сначала попробуйте быстрый
# Если не работает, тогда:
make bg-auto-full
```

### Только backend (очень быстро)
```bash
make hot-pull  # ~10 секунд
```

---

## Примеры использования

### Сценарий 1: Обычное обновление
```bash
# На сервере
cd /root/crm-medical-platform
make deploy-all
# Ждем 2-3 минуты
# Готово! ✅
```

### Сценарий 2: Изменили зависимости
```bash
# На сервере
cd /root/crm-medical-platform
git pull
make bg-auto-full
# Ждем 10-15 минут
# Готово! ✅
```

### Сценарий 3: Только backend
```bash
# На сервере
cd /root/crm-medical-platform
make hot-pull
# Ждем 10 секунд
# Готово! ✅
```

---

## Вывод

**Для 99% случаев используйте:**
```bash
make deploy-all
```

Это самая удобная и быстрая команда для обычного деплоя! 🚀
