# Быстрое обновление проекта

## 🚀 Одна команда для всего

```bash
make deploy-all
```

Эта команда:
1. ✅ Делает `git pull`
2. ✅ Обновляет backend
3. ✅ Обновляет frontend
4. ✅ Переключает трафик автоматически
5. ✅ Zero downtime (без простоя)

**Время**: ~2-3 минуты

---

## Альтернативные команды

### Только backend (быстро):
```bash
make hot-pull             # ~10 секунд
```

### Только frontend:
```bash
git pull
make bg-auto-fast         # ~2-3 минуты
```

### Полная пересборка (если что-то сломалось):
```bash
git pull
make bg-auto-full         # ~10-15 минут
```

---

## Что происходит при `make deploy-all`

1. **Git Pull** - получает последние изменения
2. **Build** - собирает новые Docker образы (с кэшем, быстро)
3. **Deploy** - запускает в неактивном окружении (blue/green)
4. **Health Check** - проверяет что всё работает
5. **Switch** - переключает трафик на новую версию
6. **Cleanup** - останавливает старую версию

**Результат**: Обновленный backend + frontend без простоя!

---

## Проверка статуса

```bash
make bg-status            # Посмотреть какая версия активна
docker ps                 # Посмотреть запущенные контейнеры
```

---

## Откат (если что-то пошло не так)

```bash
make bg-rollback          # Вернуться к предыдущей версии
```

---

## Рекомендация

**Для ежедневных обновлений используйте:**
```bash
make deploy-all
```

Это самый быстрый и безопасный способ обновить весь проект!
