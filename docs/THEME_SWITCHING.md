# Переключение тем на сайте

## Что изменилось

Теперь все пользователи видят **светлую тему по умолчанию**, независимо от настроек их системы. Каждый пользователь может самостоятельно переключить тему на темную, и его выбор сохранится.

## Где находится кнопка переключения темы

1. **Главная страница** - в верхней навигации справа
2. **Страница входа** - в правом верхнем углу
3. **Дашборд** - в боковой панели внизу, над кнопкой "Настройки"

## Как это работает

- По умолчанию все видят светлую тему
- Нажмите на кнопку с иконкой луны 🌙 чтобы переключиться на темную тему
- Нажмите на кнопку с иконкой солнца ☀️ чтобы вернуться к светлой теме
- Ваш выбор сохраняется и будет применен при следующем визите

## Технические детали

### Созданные файлы

1. `frontend/lib/theme-provider.tsx` - провайдер темы с управлением состоянием
2. `frontend/components/theme-toggle.tsx` - кнопка переключения темы
3. `frontend/app/theme-script.tsx` - скрипт для предотвращения мигания

### Измененные файлы

1. `frontend/app/layout.tsx` - добавлен ThemeProvider и ThemeScript
2. `frontend/app/globals.css` - изменен с `@media (prefers-color-scheme: dark)` на `.dark`
3. `frontend/tailwind.config.ts` - добавлен `darkMode: 'class'`
4. `frontend/components/layout/Sidebar.tsx` - добавлена кнопка переключения
5. `frontend/app/auth/page.tsx` - добавлена кнопка переключения
6. `frontend/app/page.tsx` - добавлена кнопка переключения

## Цветовая схема

### Светлая тема (по умолчанию)
- Фон: белый (#ffffff)
- Текст: темно-серый (#171717)
- Акцентный цвет: черный

### Темная тема
- Фон: почти черный (#0a0a0a)
- Текст: светло-серый (#ededed)
- Акцентный цвет: белый

## Для разработчиков

Чтобы использовать тему в новых компонентах:

```tsx
// Использование Tailwind классов
<div className="bg-white dark:bg-gray-900 text-black dark:text-white">
  Контент
</div>

// Использование хука темы
import { useTheme } from '@/lib/theme-provider';

function MyComponent() {
  const { theme, toggleTheme } = useTheme();
  // theme будет 'light' или 'dark'
}
```
