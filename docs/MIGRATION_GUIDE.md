# 🔄 Руководство по миграции на новую структуру

## ⚠️ ВАЖНО: Прочитайте перед запуском!

Структура проекта была полностью реорганизована. Следуйте этим шагам для успешной миграции.

## 📋 Что изменилось

### Старая структура ❌
```
CRM/
├── app/              # Frontend страницы
├── components/       # Frontend компоненты
├── lib/              # Frontend утилиты
├── backend/          # Backend
├── *.md             # Документация везде
└── docker-compose.yml
```

### Новая структура ✅
```
CRM/
├── frontend/         # Весь frontend в одной папке
├── backend/          # Весь backend в одной папке
├── docs/             # Вся документация в одной папке
├── Makefile          # Удобные команды
├── docker-compose.yml      # Production
└── docker-compose.dev.yml  # Development
```

## 🚀 Шаги миграции

### 1. Остановите старые контейнеры

```bash
# Остановите и удалите старые контейнеры
docker-compose down -v
```

### 2. Обновите код

```bash
# Если используете Git
git pull origin main

# Или скачайте новую версию
```

### 3. Проверьте структуру

Убедитесь, что у вас есть:
- ✅ `frontend/` папка с app, components, lib
- ✅ `backend/` папка с api, crm_backend
- ✅ `docs/` папка с документацией
- ✅ `Makefile` в корне
- ✅ `docker-compose.yml` и `docker-compose.dev.yml`

### 4. Создайте .env файлы (опционально)

```bash
# Backend
cp backend/.env.example backend/.env
# Отредактируйте backend/.env при необходимости

# Frontend
cp frontend/.env.example frontend/.env
# Отредактируйте frontend/.env при необходимости
```

### 5. Запустите проект

#### Development (рекомендуется для разработки)
```bash
make dev
```

Или без Make:
```bash
docker-compose -f docker-compose.dev.yml up --build
```

**Доступ:**
- Frontend: http://localhost:3001
- Backend: http://localhost:8001/api
- PostgreSQL: localhost:5433

#### Production
```bash
make prod
```

Или без Make:
```bash
docker-compose up --build
```

**Доступ:**
- Frontend: http://localhost:3000
- Backend: http://localhost:8000/api
- PostgreSQL: localhost:5432

### 6. Создайте суперпользователя

```bash
make createsuperuser
```

Или без Make:
```bash
docker-compose exec backend python manage.py createsuperuser
```

## 🔧 Полезные команды

### С Makefile (рекомендуется)

```bash
make help              # Показать все команды
make dev               # Запустить dev
make prod              # Запустить prod
make logs-dev          # Логи dev
make logs-prod         # Логи prod
make down-dev          # Остановить dev
make down-prod         # Остановить prod
make migrate           # Применить миграции
make clean             # Очистить все
```

### Без Makefile

```bash
# Development
docker-compose -f docker-compose.dev.yml up --build
docker-compose -f docker-compose.dev.yml down
docker-compose -f docker-compose.dev.yml logs -f

# Production
docker-compose up --build
docker-compose down
docker-compose logs -f
```

## 📊 Различия между Dev и Prod

| Параметр | Development | Production |
|----------|-------------|------------|
| Frontend порт | 3001 | 3000 |
| Backend порт | 8001 | 8000 |
| PostgreSQL порт | 5433 | 5432 |
| База данных | crm_db_dev | crm_db |
| DEBUG | True | False |
| Hot reload | Да | Нет |
| Volumes | Да (код монтируется) | Нет |

## ❓ Часто задаваемые вопросы

### Q: Мои старые данные сохранились?
A: Если вы использовали `docker-compose down -v`, данные были удалены. Если просто `docker-compose down`, то volumes остались и данные сохранены.

### Q: Порты заняты?
A: Используйте dev режим (`make dev`) - он использует другие порты (3001, 8001, 5433).

### Q: Как переключиться между dev и prod?
A: 
```bash
# Остановить dev
make down-dev

# Запустить prod
make prod

# Или наоборот
make down-prod
make dev
```

### Q: Нужно ли переносить .env файлы?
A: Нет, создайте новые на основе .env.example в папках backend/ и frontend/.

### Q: Makefile не работает на Windows?
A: Используйте команды docker-compose напрямую или установите Make для Windows.

### Q: Как обновить зависимости?
A: 
```bash
# Backend
docker-compose exec backend pip install -r requirements.txt

# Frontend
docker-compose exec frontend npm install
```

## 📚 Дополнительная документация

- [README.md](./README.md) - Главная документация
- [docs/НОВАЯ_СТРУКТУРА.md](./docs/НОВАЯ_СТРУКТУРА.md) - Подробное описание новой структуры
- [docs/DOCKER_SETUP.md](./docs/DOCKER_SETUP.md) - Инструкции по Docker
- [backend/README.md](./backend/README.md) - Backend документация
- [frontend/README.md](./frontend/README.md) - Frontend документация

## ✅ Проверка успешной миграции

После запуска проверьте:

1. ✅ Frontend открывается в браузере
2. ✅ Backend API отвечает (откройте http://localhost:8000/api или http://localhost:8001/api)
3. ✅ База данных работает (проверьте логи: `make logs-dev` или `make logs-prod`)
4. ✅ Можете войти в систему

## 🆘 Проблемы?

Если что-то не работает:

1. Проверьте логи: `make logs-dev` или `make logs-prod`
2. Остановите все: `make clean`
3. Запустите заново: `make dev` или `make prod`
4. Проверьте документацию в папке `docs/`

## 🎉 Готово!

Миграция завершена! Теперь у вас современная структура проекта с удобным управлением через Makefile.
