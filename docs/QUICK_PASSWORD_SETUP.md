# Быстрая настройка авторизации через пароль

## Для пользователей

### Как установить пароль

1. **Войдите в систему** через код из WhatsApp (как обычно)

2. **Перейдите в настройки:**
   - Нажмите на иконку профиля в правом верхнем углу
   - Выберите "Настройки"

3. **Найдите раздел "Безопасность":**
   - Прокрутите страницу вниз до раздела с замком 🔒

4. **Установите пароль:**
   - Введите новый пароль (минимум 6 символов)
   - Подтвердите пароль
   - Нажмите "Установить пароль"

5. **Готово!** Теперь вы можете входить через пароль

### Как войти через пароль

1. Откройте страницу входа
2. Выберите вкладку **"Пароль"** (вместо "Код через WhatsApp")
3. Введите номер телефона
4. Нажмите "Продолжить"
5. Введите пароль
6. Нажмите "Войти"

### Преимущества авторизации через пароль

✅ Быстрый вход без ожидания кода  
✅ Работает без интернета на телефоне  
✅ Не зависит от WhatsApp  
✅ Можно использовать на любом устройстве  

### Важно!

- OTP авторизация (через WhatsApp) остается доступной
- Вы можете использовать любой из двух способов
- Если забыли пароль - используйте OTP для входа
- Рекомендуем использовать надежный пароль

## Для разработчиков

### Что было добавлено

**Backend (Django):**
- `POST /api/users/login_with_password/` - вход через пароль
- `POST /api/users/set_password/` - установка/изменение пароля
- Обновлен `complete_registration` для опциональной установки пароля при регистрации

**Frontend (Next.js):**
- Переключатель между OTP и паролем на странице входа
- Форма входа через пароль
- Компонент `SetPasswordForm` для настроек
- Раздел "Безопасность" в настройках профиля

**Безопасность:**
- Пароли хешируются через Django's `make_password` (PBKDF2)
- Требуется старый пароль для изменения существующего
- Минимальная длина: 6 символов

### Запуск в Docker

```bash
# Перезапуск контейнеров
docker restart crm-backend-dev
docker restart crm-frontend-dev

# Проверка логов
docker logs crm-backend-dev --tail 50
docker logs crm-frontend-dev --tail 50
```

### Тестирование API

```bash
# Установка пароля
curl -X POST http://localhost:8001/api/users/set_password/ \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+77771234567",
    "new_password": "mypassword123"
  }'

# Вход через пароль
curl -X POST http://localhost:8001/api/users/login_with_password/ \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+77771234567",
    "password": "mypassword123"
  }'
```

### Файлы изменений

**Backend:**
- `backend/api/views.py` - добавлены методы `login_with_password`, `set_password`
- `backend/api/models.py` - без изменений (используется встроенное поле password)

**Frontend:**
- `frontend/app/auth/page.tsx` - добавлен выбор метода авторизации
- `frontend/components/auth/SetPasswordForm.tsx` - новый компонент
- `frontend/app/dashboard/settings/page.tsx` - добавлен раздел безопасности
- `frontend/lib/api/client.ts` - добавлены методы API
- `frontend/lib/store/user-store.ts` - добавлены методы store

**Документация:**
- `docs/PASSWORD_AUTH.md` - полная документация
- `docs/QUICK_PASSWORD_SETUP.md` - краткая инструкция
