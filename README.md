# CRM Medical Platform

Современная платформа для управления медицинскими осмотрами сотрудников с использованием Next.js и Django.

## 🏗️ Структура проекта

```
CRM/
├── backend/              # Django REST API
│   ├── api/             # API endpoints и модели
│   ├── crm_backend/     # Настройки Django
│   └── requirements.txt
├── frontend/            # Next.js приложение
│   ├── app/            # Страницы и роуты
│   ├── components/     # React компоненты
│   ├── lib/            # Утилиты и API клиент
│   └── package.json
├── docs/               # Документация проекта
├── docker-compose.yml  # Production конфигурация
├── docker-compose.dev.yml # Development конфигурация
└── Makefile           # Команды для управления проектом
```

## 🚀 Быстрый старт

### Требования
- Docker Desktop
- Make (опционально, для удобства)

> **💡 Для пользователей Windows:** Если Make не работает, смотрите [docs/WINDOWS_GUIDE.md](./docs/WINDOWS_GUIDE.md) для альтернативных команд.

### Запуск Development среды

```bash
# С использованием Make
make dev

# Или напрямую через Docker Compose
docker-compose -f docker-compose.dev.yml up --build
```

**Development сервисы:**
- Frontend: http://localhost:3001
- Backend API: http://localhost:8001/api
- PostgreSQL: localhost:5433

**Настройки:** Использует `.env.dev` файл с настройками для разработки.

### Запуск Production среды

> **⚠️ ВАЖНО:** Перед первым запуском в production отредактируйте файл `.env.prod` и измените пароли и секретные ключи! См. [docs/ENV_SETUP.md](./docs/ENV_SETUP.md)

```bash
# С использованием Make
make prod

# Или напрямую через Docker Compose
docker-compose up --build
```

**Production сервисы:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/api
- PostgreSQL: localhost:5432

**Настройки:** Использует `.env.prod` файл с настройками для production.

## 📋 Makefile команды

### Development
- `make dev` - Полный запуск в dev режиме (сборка + запуск)
- `make build-dev` - Собрать dev образы
- `make up-dev` - Запустить dev контейнеры
- `make down-dev` - Остановить dev контейнеры
- `make logs-dev` - Показать логи dev
- `make restart-dev` - Перезапустить dev

### Production
- `make prod` - Полный запуск в prod режиме (сборка + запуск)
- `make build-prod` - Собрать prod образы
- `make up-prod` - Запустить prod контейнеры
- `make down-prod` - Остановить prod контейнеры
- `make logs-prod` - Показать логи prod
- `make restart-prod` - Перезапустить prod

### Утилиты
- `make migrate` - Применить миграции Django
- `make create-admin` - Создать администратора с OTP авторизацией
- `make createsuperuser` - Создать суперпользователя (старый метод)
- `make shell-backend` - Войти в shell backend контейнера
- `make shell-frontend` - Войти в shell frontend контейнера
- `make clean` - Очистить все контейнеры и volumes
- `make status` - Показать статус контейнеров
- `make help` - Показать все доступные команды

## 🛠️ Технологии

### Backend
- Django 4.2
- Django REST Framework
- PostgreSQL 15
- Green API (WhatsApp OTP)

### Frontend
- Next.js 14
- TypeScript
- Tailwind CSS
- Framer Motion
- Recharts

## 🔐 Admin панель

Django Admin панель доступна по адресу:
- **Development**: http://localhost:8001/admin/
- **Production**: http://localhost:8000/admin/

### Авторизация через OTP

Admin панель использует OTP авторизацию через WhatsApp (как и фронтенд):

1. **Создайте администратора:**
   ```bash
   make create-admin
   # Или напрямую:
   docker-compose exec backend python manage.py create_admin_user +77001234567
   ```

2. **Войдите в admin:**
   - Откройте http://localhost:8001/admin/
   - Введите номер телефона
   - Получите код в WhatsApp
   - Введите код и войдите

> **📖 Подробная документация:** 
> - [docs/ADMIN_QUICK_START.md](./docs/ADMIN_QUICK_START.md) - Быстрый старт
> - [docs/ADMIN_OTP_AUTH.md](./docs/ADMIN_OTP_AUTH.md) - Полная документация

## 🔐 Переменные окружения

Проект использует отдельные .env файлы для разных окружений:

- **`.env.dev`** - Development (готов к использованию)
- **`.env.prod`** - Production (⚠️ требует настройки!)

### Основные переменные:

**Backend:**
- `POSTGRES_PASSWORD` - Пароль БД ⚠️
- `SECRET_KEY` - Django секретный ключ ⚠️
- `DEBUG` - Режим отладки
- `ALLOWED_HOSTS` - Разрешенные хосты
- `GREEN_API_*` - Настройки Green API для OTP

**Frontend:**
- `NEXT_PUBLIC_API_URL` - URL backend API
- `NODE_ENV` - Окружение

> **📖 Подробная документация:** [docs/ENV_SETUP.md](./docs/ENV_SETUP.md)

## 📚 Документация

Вся документация находится в папке `docs/`:
- Инструкции по запуску
- Описание функционала
- Статус проекта
- Технические детали

## 🔧 Разработка

### Локальная разработка без Docker

#### Backend
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

#### Frontend
```bash
cd frontend
npm install
npm run dev
```

## 🗄️ База данных

Проект использует отдельные БД для dev и prod:

- **Production**: `crm_db` на порту 5432
- **Development**: `crm_db_dev` на порту 5433

Обе БД работают в отдельных Docker контейнерах.

## 🔐 Переменные окружения

### Backend
- `POSTGRES_DB` - Имя базы данных
- `POSTGRES_USER` - Пользователь БД
- `POSTGRES_PASSWORD` - Пароль БД
- `POSTGRES_HOST` - Хост БД
- `SECRET_KEY` - Django secret key
- `DEBUG` - Режим отладки
- `GREEN_API_*` - Настройки Green API

### Frontend
- `NEXT_PUBLIC_API_URL` - URL backend API
- `NODE_ENV` - Окружение (development/production)

## 📦 Основной функционал

1. **Управление контингентом** - загрузка и редактирование списка сотрудников
2. **Календарные планы** - создание и утверждение планов осмотров
3. **Маршрутные листы** - автоматическая генерация с QR-кодами
4. **Электронная очередь** - управление очередью пациентов
5. **Лабораторные исследования** - ввод результатов анализов
6. **Функциональные исследования** - ЭКГ, спирометрия и др.
7. **Врачебные осмотры** - проведение осмотров специалистами
8. **Экспертиза** - вынесение вердикта профпатологом
9. **Отчеты** - сводный отчет и заключительный акт с экспортом PDF/Excel
10. **История осмотров** - просмотр предыдущих осмотров пациента

## 🤝 Роли пользователей

- **Работодатель** - управление контингентом, просмотр планов
- **Менеджер клиники** - календарные планы, заключительные акты
- **Регистратура** - маршрутные листы, электронная очередь
- **Врач** - проведение осмотров
- **Профпатолог** - экспертиза, врачебная комиссия

## 📄 Лицензия

MIT

## 🆘 Поддержка

Для получения помощи обратитесь к документации в папке `docs/` или создайте issue в репозитории.
