# Сравнение команд деплоя

## 🎯 Главное отличие

| Команда | Git Pull | Интерактивность | Когда использовать |
|---------|----------|-----------------|-------------------|
| **`make deploy-all`** | ✅ Да (автоматически) | ❌ Нет | **Ежедневные обновления** |
| **`make bg-auto`** | ❌ Нет | ✅ Да (спрашивает) | Контролируемый деплой |

---

## 📋 Детальное сравнение

### `make deploy-all` - Быстрый автоматический деплой

```bash
make deploy-all
```

**Что делает:**
1. ✅ `git pull` - автоматически
2. ✅ Быстрая сборка (с кэшем) - автоматически
3. ✅ Деплой в неактивное окружение
4. ✅ Health check
5. ✅ Переключение трафика (автоматически или с подтверждением)
6. ✅ Cleanup старого окружения

**Особенности:**
- 🚀 **Полностью автоматический** (минимум вопросов)
- ⚡ **Всегда быстрая сборка** (с Docker cache)
- 🔄 **Включает git pull**
- ⏱️ **~2-3 минуты**

**Когда использовать:**
- ✅ Ежедневные обновления кода
- ✅ Быстрые фиксы
- ✅ Когда нужно просто обновить код
- ✅ Когда зависимости не менялись

---

### `make bg-auto` - Интерактивный деплой

```bash
make bg-auto
```

**Что делает:**
1. ❌ `git pull` - НЕТ (нужно сделать вручную)
2. ❓ **Спрашивает** тип сборки:
   - Быстрая (с кэшем)
   - Полная (без кэша)
3. ✅ Деплой в неактивное окружение
4. ✅ Health check
5. ❓ **Спрашивает** подтверждение переключения
6. ❓ **Спрашивает** "Сайт работает нормально?"
7. ✅ Cleanup старого окружения

**Особенности:**
- 🤔 **Интерактивный** (задает вопросы)
- 🎛️ **Выбор типа сборки** (быстрая/полная)
- ⏸️ **Паузы для проверки**
- 🔍 **Больше контроля**
- ⏱️ **~2-15 минут** (зависит от выбора)

**Когда использовать:**
- ✅ Важные обновления
- ✅ Когда изменились зависимости (выбрать полную сборку)
- ✅ Когда нужен контроль на каждом шаге
- ✅ Первый деплой после больших изменений
- ✅ Когда хотите протестировать перед переключением

---

## 🎯 Рекомендации

### Для ежедневной работы:
```bash
make deploy-all
```
- Быстро
- Просто
- Автоматически

### Для важных обновлений:
```bash
git pull
make bg-auto
# Выбрать: 2) Полная сборка
```
- Больше контроля
- Можно выбрать полную пересборку
- Паузы для проверки

### Для обновления зависимостей:
```bash
git pull
make bg-auto-full
```
или
```bash
git pull
make bg-auto
# Выбрать: 2) Полная сборка
```

---

## 🔄 Альтернативные команды

### Быстрая сборка (автоматически):
```bash
make deploy-all          # С git pull
make bg-auto-fast        # Без git pull
```

### Полная сборка (автоматически):
```bash
git pull
make bg-auto-full        # Полная пересборка без вопросов
```

### Только backend (очень быстро):
```bash
make hot-pull            # ~10 секунд
```

---

## 📊 Таблица времени

| Команда | Git Pull | Тип сборки | Время | Вопросы |
|---------|----------|------------|-------|---------|
| `make deploy-all` | ✅ Да | Быстрая | ~2-3 мин | Минимум |
| `make bg-auto` | ❌ Нет | Спрашивает | ~2-15 мин | Много |
| `make bg-auto-fast` | ❌ Нет | Быстрая | ~2-3 мин | Средне |
| `make bg-auto-full` | ❌ Нет | Полная | ~10-15 мин | Средне |
| `make hot-pull` | ✅ Да | - | ~10 сек | Нет |

---

## 💡 Примеры использования

### Сценарий 1: Обычное обновление кода
```bash
# Изменили код backend/frontend
make deploy-all
# ✅ Готово за 2-3 минуты
```

### Сценарий 2: Обновили package.json
```bash
git pull
make bg-auto
# Выбрать: 2) Полная сборка
# ✅ Пересоберет с новыми зависимостями
```

### Сценарий 3: Критическое обновление
```bash
git pull
make bg-auto
# Выбрать: 2) Полная сборка
# Проверить на каждом шаге
# ✅ Максимальный контроль
```

### Сценарий 4: Быстрый фикс backend
```bash
# Изменили только backend код
make hot-pull
# ✅ Готово за 10 секунд
```

---

## ❓ Какую команду выбрать?

### Используйте `make deploy-all` если:
- ✅ Обычное обновление кода
- ✅ Нужно быстро
- ✅ Зависимости не менялись
- ✅ Доверяете процессу

### Используйте `make bg-auto` если:
- ✅ Важное обновление
- ✅ Изменились зависимости
- ✅ Нужен контроль
- ✅ Хотите выбрать тип сборки
- ✅ Первый раз деплоите что-то новое

---

## 🎓 Итог

**90% случаев:**
```bash
make deploy-all
```

**10% случаев (важные обновления):**
```bash
git pull
make bg-auto
```

**Экстренные случаи (только backend):**
```bash
make hot-pull
```
